---
name: commit-push-pr
description: Commit changes, push, and optionally create a PR
---
1. Run `git status` and `git diff --stat`
2. Write a descriptive commit message (WHAT changed and WHY)
3. Run `git add -A && git commit -m "[message]"`
4. Push to current branch
