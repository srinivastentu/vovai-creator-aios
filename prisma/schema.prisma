// VOVAI CreatorOS — Prisma Schema
// Models defined in CR-1 onwards. Datasource url is supplied by
// prisma.config.ts (env DATABASE_URL).

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
